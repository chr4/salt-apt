# vim: ft=yaml

apt:
  unattended-upgrades:
    # Disable unattended-upgrades
    disable: true

    # Blacklist packages to prevent automatic upgrades
    blacklist: [keepalived, vim]

    # Enable automatic updates for all origins
    all-origins: all
